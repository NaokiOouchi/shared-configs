name: Sync Files

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: NaokiOouchi/cross-repo-sync-action@v1
        with:
          token: ${{ secrets.PAT }}
